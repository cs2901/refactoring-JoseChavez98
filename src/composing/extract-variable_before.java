void renderBanner() {
    final boolean var1=platform.toUpperCase().indexOf("MAC") > -1;
    final boolean var2=browser.toUpperCase().indexOf("IE") > -1;
    final boolean var4= resize > 0 ;

  if ( var1 && var2 && wasInitialized(); && var4 )
  {
    // do something
  }
}